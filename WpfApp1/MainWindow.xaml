<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        Title="Benchmark - Измерение производительности" Height="800" Width="1200">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Width" Value="200"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,0,5,0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Измерение производительности с помощью Benchmark на разных задачах" 
                   FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Панель управления -->
        <GroupBox Grid.Row="1" Header="Параметры тестирования" Padding="10">
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Тип задачи:"/>
                    <ComboBox x:Name="TaskTypeComboBox" SelectedIndex="0">
                        <ComboBoxItem Content="Count Numbers Above Average"/>
                        <ComboBoxItem Content="Find Max/Min Identical Elements"/>
                        <ComboBoxItem Content="Find Prime Numbers"/>
                        <ComboBoxItem Content="Max/Min Even/Odd Numbers"/>
                        <ComboBoxItem Content="Average Positive/Negative Numbers"/>
                    </ComboBox>

                    <TextBlock Text="Размер данных:"/>
                    <ComboBox x:Name="SizeComboBox" Width="120" SelectedIndex="2">
                        <ComboBoxItem Content="10,000"/>
                        <ComboBoxItem Content="100,000"/>
                        <ComboBoxItem Content="1,000,000"/>
                        <ComboBoxItem Content="5,000,000"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <ProgressBar x:Name="ProgressBar" Height="10" Width="200" Margin="5" Maximum="100"/>

                    <Button x:Name="RunBenchmarkBtn" Content="Запустить тесты" 
                            Click="RunBenchmarkBtn_Click" Background="#FF008000" Foreground="White"/>
                    <Button x:Name="SaveToDbBtn" Content="Сохранить в БД" 
                            Click="SaveToDbBtn_Click" Background="#FF1E90FF" Foreground="White"/>
                    <Button x:Name="ClearResultsBtn" Content="Очистить результаты" 
                            Click="ClearResultsBtn_Click" Background="#FFDC143C" Foreground="White"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <!-- Графики -->
        <TabControl Grid.Row="2" Margin="0,10,0,0">
            <TabItem Header="Время выполнения">
                <lvc:CartesianChart x:Name="TimeChart" LegendPosition="Right" />
            </TabItem>
            <TabItem Header="Использование памяти">
                <lvc:CartesianChart x:Name="MemoryChart" LegendPosition="Right" />
            </TabItem>
            <TabItem Header="Ускорение">
                <lvc:CartesianChart x:Name="SpeedupChart" LegendPosition="Right" />
            </TabItem>
        </TabControl>

        <!-- Таблица результатов -->
        <GroupBox Grid.Row="3" Header="Детальные результаты" Margin="0,10,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" x:Name="StatusText" Text="Готов к тестированию..." 
                          FontStyle="Italic" Margin="5"/>

                <DataGrid Grid.Row="1" x:Name="ResultsDataGrid" AutoGenerateColumns="False" 
                         IsReadOnly="True" Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Метод" Binding="{Binding MethodName}" Width="2*"/>
                        <DataGridTextColumn Header="Время (ms)" Binding="{Binding ExecutionTime}" Width="*"/>
                        <DataGridTextColumn Header="Память (MB)" Binding="{Binding MemoryUsed}" Width="*"/>
                        <DataGridTextColumn Header="Результат" Binding="{Binding Result}" Width="*"/>
                        <DataGridTextColumn Header="Ускорение" Binding="{Binding Speedup}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>
    </Grid>
</Window>